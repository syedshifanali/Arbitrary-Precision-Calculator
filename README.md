# Arbitrary-Precision-Calculator
Arbitrary Precision Calculator is a C-based application that performs arithmetic operations on very large numbers beyond standard data type limits. It uses string manipulation, dynamic memory allocation, and custom algorithms to accurately handle addition, subtraction, multiplication, and division of large integers.
